mixin g-tag(tag)
	case tag
		when 'a': a(
			class= attributes.class
			href= attributes.href
		): block
		when 'span': span(class= attributes.class): block
		default: div(class= attributes.class): block