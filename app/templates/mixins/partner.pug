mixin partner(data)
	a.partner(href= data && data.link ? data.link : '#')
		.partner__img(
			style='background-image: url(' + (data && data.img ? data.img : 'img/no-photo.svg') + ');'
			role='img'
			aria-label= data && data.title ? data.title : 'Image'
		)